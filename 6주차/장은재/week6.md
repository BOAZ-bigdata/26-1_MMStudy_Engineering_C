# **전역 스냅샷은 실제 서비스에서 어떻게 구현될까?**

> Apache Flink를 중심으로
> 

<aside>

🤔 초당 수만 건의 데이터가 흐르는 실제 서비스에서는
    전역 스냅샷이 어떻게 구현될까?

</aside>

실제 스트림 처리 시스템인 Apache Flink가

전역 스냅샷을 어떻게 구현하는지 알아보자.



## **1. Flink에서 전역 스냅샷 = 체크포인트**

Flink에서 전역 스냅샷은 

**체크포인트라는 기능**으로 구현된다.

- **체크포인트란?**
    
    데이터를 처리하는 도중,
    
    각 작업이 가지고 있는  **현재 상태를 저장하는 것** 
    
    예를 들어,
    
    - 지금까지 몇 개의 이벤트를 처리했는지
    - 특정 시간 구간의 합계가 얼마인지
    - 어떤 키의 데이터가 얼마까지 모였는지
    
    이런 값들이 모두 **상태**다.
    
    체크포인트는 이 상태를  **특정 시점 기준으로 저장 해두는 것**이다.
    

작업이 중단되면

마지막 체크포인트 시점으로 복구한 뒤

그 이후 데이터부터 다시 처리한다.



## **2. 이론의 마커가 현실에서 어떻게 쓰일까?**

책에서는 전역 스냅샷을 위해 ***마커***를 사용한다고 한다.

Flink에서는 이 역할을

→ 체크포인트 장벽(Barrier)이 한다.

> 체크포인트 장벽(Barrier)이 하는 일
> 

<aside>

**체크포인트 장벽**: 일반 데이터와 함께 흐르는 하나의 특별한 이벤트

</aside>

이 장벽이 도착하면 각 작업은 그 시점의 상태를 저장한다.

정리하면,

- 장벽 **이전** 데이터 → **이번** 체크포인트에 포함
- 장벽 **이후** 데이터 → **다음** 체크포인트에 포함

장벽은 처리 경계를 나누는 **기준점**이다.

—> 책에 나오는 마커와 거의 같은 개념



## **3. 병렬 처리 환경에서의 문제**

현실의 Flink는 병렬로 동작한다.

예를 들어,

하나의 작업이 여러 개로 나뉘어 동시에 실행될 수 있고,

여러 경로에서 데이터가 들어올 수 있다.

<aside>

: 이때 생기는 문제

- 데이터가 많아서 느린 경우
- 빨라서 먼저 장벽이 도착하는 경우

→ 각 경로에서 장벽이 도착하는 시간이 다를 수 있다.

</aside>

> **정렬 과정 (Checkpoint Alignment)**
> 
- Flink는 모든 경로에서 장벽이 도착할 때까지 기다린다.
- 그리고 나서 상태를 저장한다.
    
    —> 이 과정을 정렬이라고 함.
    

정렬이 길어지면

- 일부 데이터 흐름이 멈추고
- 병목이 발생할 수 있다.



## **4. 정확히 한 번 처리의 의미**

Flink는 **Exactly-once(정확히 한 번 처리)** 모드를 지원한다고 한다.

여기서 중요한 점은,

- 이벤트가 한 번만 실행된다는 뜻이 X
- 최종 상태에 한 번만 반영된다는 뜻

- 장애가 발생하면 데이터를 다시 읽어 처리한다.
    
    → 이 과정에서 일부 이벤트는 두 번 실행될 수 있다.
    

—> 최종 상태는 체크포인트를 기준으로 일관되게 유지된다.

즉, 핵심은 

실행 횟수가 아니라 **결과의 정확성**이다.



## **5. 외부 시스템까지 생각해보면?**

Q. 만약 결과를 외부 데이터베이스에 저장한다면?
    
A. 복구 과정에서 중복 저장이 발생할 수 있다.

이를 해결하기 위해,

Flink는 두 단계 방식의 처리를 사용한다.

<aside>

1. 먼저 외부 시스템에 임시 저장
2. 체크포인트가 **완전히 성공**하면 최종 확정
</aside>

→ 즉, Flink 내부 상태와 외부 데이터베이스의 결과를 같은 기준으로 맞추려는 것이다.

단, 이 방식 역시 복잡하고 비용이 따르는 구현상의 부담이 있습니다.



## **정리**

Flink의 체크포인트는

전역 스냅샷 개념을 실제 환경에 적용한 사례다.

장벽을 기준으로 처리 경계를 나누고, 그 시점의 상태를 기록한다.

다만 현실에서는

- 병렬 처리
- 성능 저하
- 외부 시스템 연동

같은 요소를 함께 고려해야 하는 현실적인 제약이 있다.

결국,

<aside>

전역 스냅샷은 이론적인 개념으로 끝나는 것이 아니라,

실제 스트림 처리 시스템에서 체크포인트라는 방식으로 구현되고 있다.

</aside>